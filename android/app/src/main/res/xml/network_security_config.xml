<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Solo el backend de desarrollo local (ver .env.development) puede
         recibir tráfico HTTP sin cifrar. Todo lo demás, incluida la
         producción real (HTTPS), usa la configuración base por defecto. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.89</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
